## What changed

<!-- One or two sentences. -->

## Cost impact

- [ ] No change to `packages/sdk/src/pricing.ts` or the optimizer's downgrade paths
- [ ] Cost regression check passes (see the bot comment on this PR)
- [ ] If cost intentionally changed, `.cost-baseline.json` was regenerated with `npm run cost:baseline`

## Checks

- [ ] `npm run test --workspaces` passes
- [ ] Migrations (if any) are additive and re-runnable
